import React from 'react';
import './NoMatch.css';

const NoMatch = () => {
  
    return (
        <div className="no-match">
            <h3> Sorry, Route is not found! </h3>
        </div>

    );
};

export default NoMatch;